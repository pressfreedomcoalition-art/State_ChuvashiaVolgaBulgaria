# Example: https://cache.chv.blc.cab → Node on 127.0.0.1:8790
# Prefer direct TLS (no Cloudflare) for RF reachability, same idea as dao.won.onl.

server {
    listen 80;
    listen [::]:80;
    server_name cache.chv.blc.cab;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name cache.chv.blc.cab;

    # ssl_certificate     /etc/letsencrypt/live/cache.chv.blc.cab/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/cache.chv.blc.cab/privkey.pem;

    client_max_body_size 2m;

    location / {
        proxy_pass http://127.0.0.1:8790;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # CORS is set by Node (CORS_ORIGIN); do not duplicate here.
    }
}
